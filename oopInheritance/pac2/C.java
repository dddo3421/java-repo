package oopInheritance.pac2;

//패키지가 다른 클래스의 객체 인스턴스 구성 : import 해야함 
//import oopInheritance.pac1.B; //public 클래스
//import oopInheritance.pac1.A; // import 불가능(패키지가 다른 default 클래스)
public class C {
///	B b; // 패키지 달라도 import했고 public 클래스이므로 접근 가능
	//A a; // default 클래스이고 패키지가 다르기 때문에 접근 불가
}
